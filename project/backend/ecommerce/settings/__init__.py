"""
Settings package.

The active configuration is chosen via the ``DJANGO_SETTINGS_MODULE``
environment variable, e.g.::

    DJANGO_SETTINGS_MODULE=ecommerce.settings.dev    # local development
    DJANGO_SETTINGS_MODULE=ecommerce.settings.prod   # production

``manage.py`` / ``wsgi.py`` / ``asgi.py`` default to ``ecommerce.settings.dev``
so local commands work out of the box.
"""
