# ==========================================================================
# Root runtime & production dependencies for Railway / Nixpacks deployment
# ==========================================================================

# --- Core framework ---
Django==5.0.1
djangorestframework==3.14.0
djangorestframework-simplejwt==5.5.1
PyJWT==2.13.0
django-cors-headers==4.3.1

# --- Configuration ---
python-dotenv==1.0.1

# --- Database drivers & helpers ---
psycopg2-binary==2.9.12
dj-database-url==2.3.0

# --- Caching / message broker client ---
redis==5.2.1
django-redis==5.4.0

# --- Asynchronous task processing ---
celery==5.4.0
django-celery-beat==2.7.0
django-celery-results==2.5.1

# --- API tooling: filtering, schema/docs ---
django-filter==24.3
drf-spectacular==0.27.2

# --- Category taxonomy (tree structures) ---
django-mptt==0.16.0

# --- Images & Storage ---
Pillow==10.2.0
django-storages==1.14.6
boto3==1.43.75

# --- Payments & HTTP ---
stripe==11.1.0
requests==2.32.3

# --- Password hashing ---
argon2-cffi==23.1.0

# --- Two-Factor Authentication (2FA) ---
pyotp==2.9.0
qrcode==7.4.2

# --- WSGI Server & Production Utilities ---
gunicorn==23.0.0
uvicorn[standard]==0.34.0
whitenoise==6.8.2
